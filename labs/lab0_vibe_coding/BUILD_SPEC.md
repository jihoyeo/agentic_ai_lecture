# 빌드 스펙 — 수도권 통근 흐름 지도 + 채팅

> 이 문서는 AI 코딩 에이전트(**Codex**, Claude Code, Cursor …)에게 줄 **요청서**입니다.
> 미리 만들어 둔 앱은 없습니다 — 이 스펙을 보고 여러분이 직접 바이브코딩으로 만듭니다.
>
> ⭐ **이 문서가 얼마나 구체적인지에 주목하세요.** 좋은 결과물은 이만큼 상세한
> 스펙에서 나옵니다. 여러분이 나중에 자기 서비스를 만들 때도 이렇게 써야 합니다.

---

## 0. 한 줄 소개

수도권 18개 도시의 **통근 흐름을 지도에 시각화**하고, 그 옆 **채팅창**에서
데이터에 대해 자연어로 질문하면 답해 주는 **한 페이지 웹 앱**.

## 1. 화면 레이아웃

화면을 좌우로 나눕니다. 왼쪽은 지도, 오른쪽은 채팅창입니다.

```
┌─────────────────────────────────────┬───────────────────────┐
│                                     │   수도권 통근 도우미    │
│                                     │                       │
│            지 도 (deck.gl)          │   ┌─────────────────┐ │
│                                     │   │ 사용자 / 봇 대화 │ │
│      · 도시 = 점                     │   │   말풍선들       │ │
│      · 통근 흐름 = 곡선(arc)         │   │                 │ │
│                                     │   └─────────────────┘ │
│                                     │   [ 입력창      ][전송]│
│         (전체 너비의 약 65%)         │     (약 35%)          │
└─────────────────────────────────────┴───────────────────────┘
```

- 데스크톱 브라우저 기준. 전체 화면을 꽉 채운다.
- 지도와 채팅창은 항상 같이 보인다 (한 화면, 스크롤 없음).

## 2. 내가 가진 데이터

데이터는 `data/` 폴더에 있는 JSON 파일 2개입니다. (실제 연구 프로젝트
`mega-region-ai` 의 수도권 예제 데이터입니다.)

### `data/cities.json` — 도시 18개

각 도시의 이름과 좌표. 배열의 각 원소:

| 필드 | 뜻 | 예 |
|---|---|---|
| `id` | 도시 코드 (흐름 데이터가 이 값으로 도시를 가리킴) | `"SEO"` |
| `name` | 영문 이름 | `"Seoul"` |
| `name_ko` | 한글 이름 | `"서울"` |
| `lat` | 위도 | `37.5665` |
| `lon` | 경도 | `126.9780` |

```json
{ "id": "SEO", "name": "Seoul", "name_ko": "서울", "lat": 37.5665, "lon": 126.9780 }
```

### `data/flows.json` — 통근 흐름 48개

도시에서 도시로 가는 통근 통행량. 배열의 각 원소:

| 필드 | 뜻 | 예 |
|---|---|---|
| `origin` | 출발 도시 `id` | `"SEO"` |
| `dest` | 도착 도시 `id` | `"INC"` |
| `count` | 통근 통행량 (예제용 상대값) | `520` |

```json
{ "origin": "SEO", "dest": "INC", "count": 520 }
```

> `flows` 의 `origin`/`dest` 는 `cities` 의 `id` 와 연결됩니다.
> 흐름은 **방향이 있습니다** — `SEO→INC` 와 `INC→SEO` 는 서로 다른 항목입니다.

## 3. 지도 — 프론트엔드 요구사항

- **프레임워크**: React + Vite 로 구현한다.
- **지도 시각화**: **deck.gl** 을 사용한다 (`@deck.gl/react` 의 `<DeckGL>`).
- **베이스맵**: **VWorld(브이월드)** 지도를 사용한다.
  - VWorld 는 국토교통부의 지도 서비스다 (<https://www.vworld.kr>).
  - API 키를 무료로 발급받아, 래스터 타일을 deck.gl 의 `TileLayer` 로 깐다.
  - 타일 URL 형식:
    `https://api.vworld.kr/req/wmts/1.0.0/{KEY}/Base/{z}/{y}/{x}.png`
  - 키는 `.env` 에 `VITE_VWORLD_KEY` 로 보관한다.
- **도시 표시**: `ScatterplotLayer` 로 각 도시를 **점**으로 그린다.
  - 점 크기는 그 도시의 **총 통근량**(들어오고 나가는 `count` 합계)에 비례한다.
  - 점을 마우스로 올리면 **툴팁**에 도시 한글 이름과 총 통근량을 보여준다.
- **흐름 표시**: `ArcLayer` 로 각 통근 흐름을 출발지→도착지 **곡선**으로 그린다.
  - 곡선 굵기는 `count` 에 비례한다.
- **초기 화면**: 수도권 전체가 보이도록 한다 (대략 경도 127.0, 위도 37.4, 줌 8~9).

## 4. 채팅 — AI Agent 요구사항

### 4-1. 역할

채팅창의 AI는 **"수도권 통근 데이터 안내 도우미"** 다.
위 두 데이터(`cities`, `flows`)에 대한 질문에 답하는 것이 유일한 임무다.

### 4-2. 동작 — 가장 중요

- **데이터에 그대로 있는 것을 물으면** → 데이터에서 찾아 **바로** 답한다.
  - 예: "서울에서 인천으로 가는 통근량은?" → `flows` 에서 `SEO→INC` 를 찾아 답.
- **계산이 필요한 것을 물으면** → 데이터로 **직접 계산해서** 답한다. *추측하지 않는다.*
  - 예: "서울로 들어오는 통근량 총합은?" → `dest` 가 `SEO` 인 모든 `count` 를 더한다.
- 데이터에 없는 것을 물으면 → 모른다고 솔직히 말한다 (지어내지 않는다).

### 4-3. 응답 규칙

- 항상 **한국어**로, 2~4문장으로 간결하게 답한다.
- 숫자를 답할 때는 어떻게 얻은 값인지 한 줄로 함께 밝힌다
  (예: "서울로 들어오는 통근량은 총 4,040입니다 — 도착지가 서울인 17개 흐름의 합계").
- 친근하고 쉬운 말투를 쓴다.

### 4-4. 예시 대화 — 아래처럼 동작해야 한다

**조회형 (데이터에 그대로 있음)**

| 사용자 | 기대 동작 / 응답 |
|---|---|
| "서울에서 인천으로 가는 통근량은?" | `SEO→INC` 를 찾아 → "520입니다." |
| "데이터에 어떤 도시들이 있어?" | `cities` 18개를 한글 이름으로 나열 |
| "용인은 어디쯤이야?" | `cities` 에서 용인 좌표를 찾아 위치를 설명 |

**계산형 (직접 계산해야 함)**

| 사용자 | 기대 동작 / 응답 |
|---|---|
| "서울로 들어오는 통근량 총합은?" | `dest==SEO` 인 `count` 를 모두 더해 답 |
| "통근량이 가장 많은 구간은?" | 모든 흐름 중 `count` 최대를 찾아 답 (서울→성남, 540) |
| "수원과 용인 사이 왕복 통근량은?" | `SUW→YIG`(260) + `YIG→SUW`(250) = 510 |

### 4-5. 구현 힌트

- 데이터가 작으므로(흐름 48개), 채팅 요청이 올 때 **`cities` 와 `flows` 전체를
  LLM에 함께 전달**해 LLM이 데이터를 직접 보고 답하게 하는 방식이 가장 간단하다.
- 계산의 **정확도가 중요**하다. 합계·최댓값 같은 계산은 자바스크립트로 먼저
  계산한 값을 LLM에 함께 넘겨주면 더 안전하다. (둘 중 편한 방식을 택한다.)
- (선택) 답에 등장하는 도시·흐름을 지도에서 잠깐 강조해 주면 더 좋다.

## 5. 기술 스택 / 제약

- 언어/프레임워크: **React + Vite** (자바스크립트 또는 타입스크립트).
- 지도: **deck.gl**, 베이스맵 **VWorld**.
- LLM: **Google Gemini** 무료 티어, 모델 `gemini-2.5-flash`.
  무료 키 발급: <https://aistudio.google.com/apikey>.
- **API 키 보안**: `GEMINI_API_KEY` 와 `VITE_VWORLD_KEY` 는 `.env` 에 둔다.
  Gemini 키는 **브라우저 코드에 절대 노출하지 않는다** — Vite 개발 서버의
  프록시(미들웨어)를 통해 호출한다.
- 화면 한 페이지로 끝낸다. 별도 UI 라이브러리는 최소화한다.

## 6. 단계적 빌드 계획 — 이 순서로 Codex에 요청한다

한 번에 다 만들지 않는다. 한 단계가 끝나면 직접 실행해 보고 다음으로 넘어간다.

1. Vite + React 프로젝트를 만들고, `data/` 의 두 JSON을 불러와 콘솔에 출력한다.
2. deck.gl + VWorld 베이스맵을 띄운다 (아직 데이터 표시는 없음).
3. 도시를 `ScatterplotLayer` 점으로, 흐름을 `ArcLayer` 곡선으로 지도에 그린다.
4. 오른쪽에 채팅 패널 UI를 붙인다 (말풍선 + 입력창, 아직 LLM 없음).
5. Gemini를 연결한다 — `.env` 키 + Vite 프록시. 데이터에 대한 질문에 답하게 한다.
6. §4-4 의 예시 대화 6개를 모두 넣어 보고, 틀리면 고친다.

## 7. 범위 — 이번에 안 만드는 것

로그인·회원, 데이터베이스, 데이터 업로드 기능, 모바일 전용 화면, 여러 페이지,
사용자별 저장. → 모두 제외. 위 6단계가 끝나면 완성이다.

---

## 부록 — Codex에게 보내는 법

이 스펙 전체를 한 번에 던지지 말고, 이렇게 시작하세요:

> "아래는 내가 만들 웹 앱의 빌드 스펙이야. 먼저 전체를 읽고, **6단계 중 1단계만**
>  구현해줘. 바로 코딩하기 전에 어떻게 만들지 계획을 먼저 말해줘.
>  〔여기에 이 BUILD_SPEC.md 전체를 붙여넣기〕"

그다음 "2단계 진행해줘", "3단계 …" 로 하나씩 넘어갑니다. 각 단계마다 직접
실행해 확인하세요. 문제가 있으면 *구체적으로* 알려 주세요 — "안 돼요" 가 아니라
"질문 A를 넣었더니 B가 나왔는데 C가 맞아요" 처럼.
